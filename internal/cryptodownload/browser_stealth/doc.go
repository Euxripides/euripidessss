// Package browserstealth provides headless browser utilities for handling
// pages that require JavaScript execution (e.g. Cloudflare challenges).
// These modules are stubs intended as technical reserves; they are not wired
// into the main scrape path by default.

package browserstealth
